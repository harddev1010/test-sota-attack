# Install with:  pip install -r requirements.txt
# (CPU works; a CUDA GPU makes the attacks much faster but is not required.)
torch>=2.2.0         # deep-learning engine (tensors + autograd the attacks rely on)
torchvision>=0.17.0  # EfficientNetV2-L + its official ImageNet weights/preprocessing
foolbox>=3.3.0       # SOTA Fast Minimum-Norm (FMN) attack — composed, not reinvented
pillow>=10.0.0       # image <-> PNG (the exact round-trip the validator does)
numpy>=1.26.0        # array math for image decode/encode
# NOTE: scikit-image is intentionally NOT used — we replicate the validator's OWN
# avg-pool SSIM and PSNR (in flipper.py) so local scores match the subnet exactly.
